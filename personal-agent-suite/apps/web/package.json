{
  "name": "@agent-suite/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sh -c 'next dev --hostname 0.0.0.0 --port ${PORT:-3000}'",
    "build": "next build",
    "start": "sh -c 'next start --hostname 0.0.0.0 --port ${PORT:-3000}'",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@agent-suite/config": "workspace:*",
    "@agent-suite/shared-types": "workspace:*",
    "next": "^15.3.3",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.5"
  }
}
